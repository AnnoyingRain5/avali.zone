{% extends "base.jinja" %}

{% block title %} Community {% endblock title %}
{% block description %} Find an avali community group, and hang out with fellow avali! {% endblock description %}

{% block header %}
<h1>The Avali Community</h1>
{% endblock header %}

{% block content %}
<div class="content">
    <p>
        The avali community is spread wide, among many different platforms, including a subreddit,
        a couple discord servers, VRChat meetups, Resonite and even a Minecraft server! <br>
        This section of the website aims to act as a hub, allowing all avali to find their bretheren,
        no matter where they are.
    </p>
</div>
{% for category in categories %}
<div class="content">
    <h2>{{category['name']}}</h2>
    <div class="dock">
        {% for group in groups if group['categoryid'] == category['id'] %}
        <div class="pod">
            <h3>{{group['name']}}</h3>
            <p>{{group['description']}}</p>
            <div class="dock">
                {% for link in links if link['infoboxid'] == group['id']%}
                <div class="pod">
                    <a href='{{link["destination"]}}'>{{link["name"]}}</a>
                </div>
                {% endfor %}
            </div>
        </div>
        {% endfor %}
    </div>
</div>
{% endfor %}
{% endblock content %}